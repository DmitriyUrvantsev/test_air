import 'package:flutter/material.dart';

class DifficultRoute extends StatelessWidget {
  const DifficultRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Placeholder(
        child: Center(child: Text('DifficultRoute')),
      ),
    );
  }
}
