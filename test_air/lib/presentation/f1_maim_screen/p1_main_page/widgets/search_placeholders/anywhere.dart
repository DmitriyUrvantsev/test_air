import 'package:flutter/material.dart';

class Anywhere extends StatelessWidget {
  const Anywhere({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Placeholder(
        child: Center(child: Text('Anywhere')),
      ),
    );
  }
}
