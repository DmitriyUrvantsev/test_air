import 'package:flutter/material.dart';

class HotTickets extends StatelessWidget {
  const HotTickets({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: Placeholder(
        child: Center(child: Text('HotTickets')),
      ),
    );
  
  }
}