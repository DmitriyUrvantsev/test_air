import '../../../../core/app_export.dart';
import 'chips_item_model.dart'; // ignore_for_file: must_be_immutable

// ignore_for_file: must_be_immutable
class K4Model {
  List<ChipsItemModel> chipsItemList =
      List.generate(3, (index) => ChipsItemModel());
}
