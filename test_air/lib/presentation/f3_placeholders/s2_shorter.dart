import 'package:flutter/material.dart';

class ShorterPage extends StatelessWidget {
  const ShorterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(
      child: Center(child: Text('ShorterPage')),
    );
  }
}
